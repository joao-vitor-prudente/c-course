#include <stdio.h>
#include <math.h>

int main(int argc, char const *argv[])
{
    double a = sqrt(2);
    int b = pow(2, 3);
    int c = round(2.5);
    int d = ceil(2.1);
    int e = floor(2.9);
    // also log, fabs, fmod, sin, cos, tan, asin, acos, atan, sinh, cosh, tanh, asinh, acosh, atanh

    return 0;
}
